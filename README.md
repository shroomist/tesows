barebones websocket that accepts messages and swaps space chars for $.
## Usage
`npm install` installs dependencies (ws, wscat)
`npm start` to start a server
`npm test` to run wscat and input messages
